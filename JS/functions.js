function whereTo() {
    switch (map) {
        case 0:
            alert('Isso é uma parede, seu cego!')
            break;
        case 1:
            alert('Você segue por um corredor!')

        default:
            break;
    }
}

function changeImage() {
    switch (map) {
        case 0:
            
            break;
    
        default:
            break;
    }
}